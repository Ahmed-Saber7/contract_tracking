<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="contract_tracking.list">
      <field name="name">contract_tracking list</field>
      <field name="model">contract_tracking.contract_tracking</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="contract_tracking.action_window">
      <field name="name">contract_tracking window</field>
      <field name="res_model">contract_tracking.contract_tracking</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="contract_tracking.action_server">
      <field name="name">contract_tracking server</field>
      <field name="model_id" ref="model_contract_tracking_contract_tracking"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="contract_tracking" id="contract_tracking.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="contract_tracking.menu_1" parent="contract_tracking.menu_root"/>
    <menuitem name="Menu 2" id="contract_tracking.menu_2" parent="contract_tracking.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="contract_tracking.menu_1_list" parent="contract_tracking.menu_1"
              action="contract_tracking.action_window"/>
    <menuitem name="Server to list" id="contract_tracking" parent="contract_tracking.menu_2"
              action="contract_tracking.action_server"/>
    -->
  </data>
</odoo>